import 'package:flutter/material.dart';

class AttributesScreen extends StatelessWidget {
  const AttributesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Attributes Screen");
  }
}
